/*
 * `/profile/exchanges` рисует тот же `ExchangePageContent`, что и `/exchange`,
 * поэтому и скелетон обязан быть тем же. Переэкспорт, а не копия: две копии
 * разъехались бы при первой же правке разметки обменов, и один и тот же экран
 * стал бы вести себя по-разному в зависимости от адреса, по которому на него
 * пришли.
 */
export { default } from "@/app/exchange/loading";
